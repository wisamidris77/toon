name: toon_dart
description: A compact, human-readable data serialization format similar to JSON but designed for configuration files and structured data.
version: 1.0.0
repository: https://github.com/google/toon_dart

environment:
  sdk: ^3.0.0

topics:
  - serialization
  - configuration
  - data-format
  - human-readable

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
